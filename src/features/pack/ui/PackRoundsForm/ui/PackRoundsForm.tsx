import {Details} from '@shared/ui-kit/Details';
import {RoundForm} from './RoundForm';

const PackRoundsForm = () => {
  return (
    <Details open header={<RoundForm />}>
      123
    </Details>
  );
};

export {PackRoundsForm};
